/target

# nWave / Claude Code: local logs, coverage output, transient DES session state
.claude/*.log
# Written by devenv's claude.code integration on every shell entry (symlinks
# into the nix store). They only appear now that devenv.nix evaluates without a
# devenv_shared checkout, and an untracked file here blocks a worktree reap.
.claude/settings.json
.mcp.json
lcov.info
**/.develop-progress.json

### Rust
# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

.direnv

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

result

.direnv*
.direnv/bin
.direnv/nix-profile-25.05-3y2jb4nbmrxx5qwh
.direnv/nix-profile-25.05-3y2jb4nbmrxx5qwh.rc


.idea/*
.idea
# nWave activation marker (keep .nwave/local-config.json trackable)
.nwave/*
!.nwave/local-config.json
